language: python
python:
  - 3.7
install:
 - pip install -q pipenv codecov
 - pipenv sync --dev
 - cp contrib/.env-sample .env
script:
 - pipenv run flake8
 - pipenv run pytest --cov=pypro
after_success:
 - pipenv run codecov
